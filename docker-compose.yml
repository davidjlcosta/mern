version: '3.8'

services:
  mongodb:
    image: mongo:6-jammy
    ports:
      - 27017:27017
    volumes:
      - mongovolume:/data/db
    networks:
      - backend
  
  api:
    depends_on:
      - mongodb
    build: ./api
    ports:
      - 8085:8085
    networks:
      - backend

volumes: 
  mongovolume:

networks:
  backend: